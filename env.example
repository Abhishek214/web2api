# URL of the LLM web app you want to proxy
TARGET_URL=https://gemini.google.com

# Port that Chrome is listening on for remote debugging
# Launch Chrome with: chrome.exe --remote-debugging-port=9222
CHROME_DEBUG_PORT=9222

# FastAPI server settings
HOST=0.0.0.0
PORT=8000
